# Task 7 report — signature Human / Agent evidence lens

## RED evidence

- The first focused route/head/bridge run failed in three places: the Human / Agent head still inherited Company machine identity, and the Home/Product bridges did not expose the live category-record relationship.
- The new lens and page suites initially failed because the dedicated modules did not exist. The stylesheet guard initially failed because `human-agent.css` was neither present nor imported.
- A review-specific RED test then proved the 12 particles had one fixed decorative orbit across all three states: 8 tests passed and 1 failed (`particleSignatures.size` was 1 rather than 3).
- GREEN bound particle radius and opacity to the same selected-layer geometry map without increasing their count; reduced motion still removes animation and transition movement.

## Implementation

- Migrated only `/human-agent` to `EnglishSiteShell pageKey="human-agent"`, exact `GLOBAL_EN_HUMAN_AGENT_PAGE` copy, and the canonical public category fact.
- Built three nested native spatial controls: outer Human conclusion, middle Evidence, and inner Agent record. Pointer, touch, Enter, Space, both directional axes, Home, End, forward wrap, and reverse wrap select the layers without autoplay.
- Selection changes real ring scale, optical depth, radial focal mask, attachment emphasis, information density, projection position, and purposeful particle radius/opacity. The resulting `data-v1-state`, geometry, depth, mask, and density signatures are distinct for Human, Evidence, and Agent.
- SSR/no-JS includes all three complete projections. Every projection retains the same canonical claim, stable ID, source ID/label, scope, review timestamp, and boundary. Hydration progressively focuses one projection without tab or application semantics.
- Human exposes what happened, why it matters, and what to review next; Evidence exposes source, scope, market/language, reviewed time, and boundary; Agent exposes claim, stable ID, source, scope, timestamp, boundary, and the live category anchor.
- The explicit discovery boundary remains reachable and does not promise crawling, retrieval, ranking, recommendation, or citation.
- Upgraded Home and Product with compact three-layer spatial bridges tied to `yonaris.category.ai-native-martech`, direct `/human-agent`, and `/agent#yonaris.category.ai-native-martech`. Neither bridge duplicates the full interaction, and no `/agent/human-agent` link is emitted.
- Corrected the public Human / Agent head minimally: canonical/hreflang and WebPage URL/ID stay on `/human-agent`, the graph is filtered to the category fact, and the live Agent HTML alternate is added while existing markdown/catalog discovery remains intact.
- Imported `src/styles/site-v1/human-agent.css`; build regeneration added the route tree entry. Legacy Human/Agent component APIs, Chinese routes, machine-document implementation, `site-06.css`, Contact, and deploy files were not changed.
- Added `scripts/human-agent-lens-layout.mjs` as a reproducible 1440x1000 / 390x844 three-state, reduced-motion, and no-JS production probe.

## Initial production visual observations

- A fresh local production build was inspected with the in-app browser before the bounded review was stopped.
- At 1440x1000, the Human state showed the lens entering above the fold beneath the exact hero. Document width was 1425/1425 with no horizontal overflow; the lens was x=37.4, width=1350.1 and all observed controls/projection stayed inside it horizontally.
- At 390x844, Human, Evidence, and Agent were selected directly. The document width was 375/375 in every measured state, with no horizontal overflow. Evidence and Agent active projections were approximately x=16, width=343, right=359, matching the lens bounds.
- Observed state signatures were `answer-orbit-expanded / outer-forward / answer-wide / narrative`, `evidence-orbit-offset / middle-forward / evidence-cross-section / contextual`, and `record-core-compressed / inner-forward / record-core / structured`. The active attachment reached a 2px stroke in the Agent state.
- Mobile optics and the active projection header remained readable within the 390x844 viewport; selecting a control kept all three native controls visible and revealed the corresponding projection immediately below the optic field.
- Automated screenshot/metrics artifact capture was not run after the final particle refinement because the installed Playwright package expects a missing Chromium headless-shell 1228 executable. The prepared probe parses successfully and is left for the independent reviewer to run in a provisioned browser environment. Reduced-motion direct access and SSR/no-JS completeness remain covered by the focused real-DOM/server-render tests.
- The known local preview PID was stopped. No deployment was performed.

## Verification

- Focused GREEN: 6 files / 52 tests passed.
- Affected GREEN: 10 files / 56 tests passed.
- Full GREEN: 55 files / 397 tests passed.
- `pnpm.cmd test:scripts`: completed in full; 40/45 passed. One baseline audit assertion still expects the already-migrated `/casework` handler to be absent, and four Site 06 visual-matrix tests cannot find the environment's Playwright Chromium headless-shell 1228 executable. Task 7 production/unit suites are green; no legacy audit expectation or dependency installation was changed in this task.
- `pnpm.cmd build`: exit 0 for client, SSR, and Nitro; only the existing large-chunk advisory remains.
- `pnpm.cmd check-types`: exit 0, run after the production build.
- `node --check scripts/human-agent-lens-layout.mjs`: exit 0.
- `git diff --check`: exit 0.

## Commit

- The clean task commit SHA is reported in the handoff (a commit cannot contain its own SHA).
